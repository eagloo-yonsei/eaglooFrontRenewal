import React from 'react';
import styled from 'styled-components';

const Page_Feedback = () => {
  return <StyledWrapper>피드백</StyledWrapper>;
};

export default Page_Feedback;

const StyledWrapper = styled.div``;
